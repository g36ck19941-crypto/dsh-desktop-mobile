# 远程遥控说明（手机远程连接电脑）

手机通过 **Tailscale 点对点网络** 远程访问电脑上的 DSH。

## 链路原理

```
手机  →  Tailscale(HTTPS, 443)  →  gzip 压缩代理(3081)  →  dsh(3080)
```

- Tailscale 提供加密点对点通道 + HTTPS 证书；
- gzip 代理把 dsh 的历史响应压缩（约 10MB → 1MB），手机加载更快。

## 前置条件

1. 电脑和手机都安装 **Tailscale**，并登录**同一个账号**；
2. 电脑已启动 DSH 桌面端。

## 一键配置（推荐）

1. 打开 DSH 桌面端；
2. 托盘右键 →「📱 手机连接」；
3. 弹出 UAC 提权，点「是」；
4. 完成后弹窗会显示手机要填的地址（形如 `https://xxx.ts.net`）。

## 手机端设置

1. 打开 DSH 手机端 → 右上角「设置」；
2. 「远程桌面地址」填电脑的 Tailscale 地址（例如 `https://asus.tail5cb106.ts.net`）；
3. 切到「远程桌面」标签，顶部显示「● 在线」即连接成功。

## 手动配置（备用）

管理员 PowerShell 执行：

```powershell
# 1) tailscale 反代到 gzip 代理
tailscale serve --bg 3081

# 2) 写入 trusted-host（把域名换成你的 MagicDNS 名）
Set-Content -Path "$env:USERPROFILE\.dsh\trusted-host.txt" -Value "你的MagicDNS名" -Encoding Ascii

# 3) 启动 gzip 代理（桌面端通常已自动启动）
node C:\dsh\gzip-proxy.js
```

## 常见问题

- **显示「离线」**：检查电脑和手机 Tailscale 是否都在线、同一账号；
- **403 / 黑屏**：确认 trusted-host.txt 里的域名和手机填的地址一致；
- **历史加载慢 / 失败**：确认 `tailscale serve` 指向 3081（gzip 代理），会自动压缩历史；
- **获取你的地址**：管理员终端运行 `tailscale serve status`，输出里的 `https://xxx.ts.net` 即手机要填的地址。

## 技术细节

- 信任栅栏：dsh 的 `/api` 会校验 Host 头，只放行回环地址和 trusted-host 里声明的域名，防止 DNS 重绑定攻击；
- 桌面端启动时会**自动校验并修复** tailscale serve 指向，避免配置漂移导致连接失败。
